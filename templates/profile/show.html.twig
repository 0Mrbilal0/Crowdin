{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <h1>Profile</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ profile.id }}</td>
        </tr>
        <tr>
            <th>Name</th>
            <td>{{ profile.username }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ profile.Email }}</td>
        </tr>
        <tr>
            <th>Langue</th>
            <td>{{ profile.langue }}</td>
        </tr>
        </tbody>
    </table>
    <a href="{{ path('app_profile') }}">back to list</a>

    <a href="{{ path('app_profile_edit', {'id': profile.id}) }}">edit</a>
{% endblock %}
